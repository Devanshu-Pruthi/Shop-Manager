import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taranmobiles.shopmanager',
  appName: 'Shop Manager',
  webDir: 'dist/demo/browser'
};

export default config;
